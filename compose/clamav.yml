---
services:
  av:
    image: ${CLAMAV_IMAGE:-clamav/clamav:latest}
    container_name: seafile-clamav
    restart: unless-stopped
    networks:
      - seafile-net

networks:
  seafile-net:
    name: seafile-net
